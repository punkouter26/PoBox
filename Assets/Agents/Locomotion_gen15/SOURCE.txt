boxer_locomotion15, final model (step 18,003,456). Full 18M-step run.

THE FIRST GENUINE ALTERNATING GAIT IN THIS PROJECT. Verified in inference over
31,901 earning steps across 16 fighters, matching the training summaries:

  SingleSupportMean  0.275   (training 0.278)
  Alternation        0.736   (training 0.724)  = a stance change every ~0.95 s
  SpeedMatchMean     0.885   (training 0.882)  at 0.46 m/s commanded
  ClearanceMean      0.346   (true ground clearance)
  1st fall           85 steps (1.7 s); ~35 stumbles per 3,000-step episode
  final lesson       Step (0.6 m/s)

Alternation counts STANCE CHANGES -- single support on one foot following
single support on the other -- so unlike SingleSupportMean it cannot be
collected by holding one leg in the air, which is exactly how gen 13 scored
0.951. The swing-foot census here is mixed (right 2 / left 4) where gen 13's
was 15 of 16 right.

NOT CONTEST-WINNING: it takes about two steps between topples. It is used by
SCN_TEST_WALK_CONTEST anyway, because gen 13 covers the same ground by hopping
on one leg and this at least looks like walking.

  gen 13   support 0.951, alternation ~0     one-legged hop, reached Walk
  gen 14   support 0.087, symmetry 0.720     went back to standing (my bug)
  gen 15   support 0.275, alternation 0.736  real gait, stalled at Step

Gen 14's symmetry term measured load balance via an EMA updated only during
single support, so a fighter on both feet never moved its bias off zero and was
paid in full for never stepping. Gen 15 counts changes instead: standing scores
0, hopping scores 0, only swapping scores.

127 observations, ground-relative heights. Same contract as Locomotion_gen9.

Full run kept at results/boxer_locomotion15/.
